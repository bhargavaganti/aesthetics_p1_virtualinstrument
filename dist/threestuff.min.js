"use strict";function init(){scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,1,1e4),camera.position.z=3e3,camera.position.y=800,camera.rotation.x=-20*Math.PI/180,c_x=camera.position.x,c_z=camera.position.z;var e=new THREE.PointLight(16711680,1,100);e.position.set(50,50,50),scene.add(e),geometry=new THREE.BoxGeometry(100,100,100);new THREE.MeshBasicMaterial({color:16711680,wireframe:!0}),new THREE.MeshBasicMaterial({color:49151,wireframe:!0});renderer=new THREE.WebGLRenderer,renderer.setSize(window.innerWidth,window.innerHeight),composer=new THREE.EffectComposer(renderer),renderScene=new THREE.RenderPass(scene,camera),renderScene.clear=!0,renderScene.renderToScreen=!0,composer.addPass(renderScene);var r=new THREE.ShaderPass(THREE.RGBShiftShader);r.renderToScreen=!0,composer.addPass(r),document.body.appendChild(renderer.domElement)}function animate(){requestAnimationFrame(animate);var e=1e-4*Date.now();camera.position.x=1500*Math.cos(e),camera.position.z=1500*Math.sin(e),camera.lookAt(scene.position),renderer.render(scene,camera)}var scene,camera,renderer,composer,renderScene,c_x,c_z;init(),animate();